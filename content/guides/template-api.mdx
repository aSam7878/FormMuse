---
title: Template API
description: Understand the shared props, values, validation, submission, and reset contract.
---

# Template API

Every FormMuse template exposes a small shared API for `onSubmit`, `defaultValues`, `className`, exported schemas, values types, submission state, retry, success persistence, and reset behavior.

Template-specific props stay limited to reusable content slots that are documented by the validated registry metadata.
