---
title: Connecting a Backend
description: Connect FormMuse templates to adopter-owned form handlers and services.
---

# Connecting a Backend

FormMuse templates do not ship a backend, runtime service, or provider-specific submission layer.

Adopters connect the template's parsed values to their own endpoint, form service, server action, analytics policy, and error handling.
